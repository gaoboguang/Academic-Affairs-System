# 同步板（2026-04-14 初始版）

> 使用规则：
> 1. 每个会话只改自己的那一行和自己的日志段落
> 2. 时间统一写 `UTC+08`
> 3. 不要整页重写
> 4. 发现会影响别的会话的事，先写到 `影响说明`，再继续

---

## 1. 当前全局状态

- 总策略：Mac 主应用主线，Windows 数据库权威线
- 主库写权限：仅 `WIN_DB_CORE`
- 数据库冻结后才能回迁到 Mac
- 当前重点：
  - Mac：Stage B 应用层深化 + 安全与导出边界修复
  - Windows：B 尾差收口 + C 人工复核推进

---

## 2. 工作流总表

| Stream | Device | Surface | 当前目标 | Owned paths | No-touch | 当前状态 | 阻塞 | 最后更新 |
|---|---|---|---|---|---|---|---|---|
| MAC_MAINLINE | Mac | Codex CLI | Stage B 应用层、导出/打印/摘要统一、安全边界修复 | `apps/frontend/**`, `apps/backend/app/api/**`, `apps/backend/app/services/**`, `apps/backend/tests/**`, `docs/**`, `README.md`, `memory-bank/**` | Windows 主库、Windows C 队列、Windows patch 合并 | 已完成 10 个摘要/规则提示闭环 | 无 | UTC+08 2026-04-15 10:43 |
| WIN_DB_CORE | Windows | Codex app | 主库单写、迁移、审计、patch 合并、DB RC | `data/local_edu_tool/**`, `apps/backend/alembic/**`, `apps/backend/app/importers/**`, `apps/backend/scripts/**`, `docs/dev/**`, `output/db_release/**` | Mac 主应用大页面重构；A/B 分片原始任务区 | 待启动 | 无 |  |
| WIN_DATA_CURATION_A | Windows | Codex app | B 尾差 A 分片 + C 队列 A 分片 | `data/gaokao_staging/**` 中 A 分片工作文件，`output/review_patches/curation_a/**`, `docs/dev/review_logs/curation_a.md` | 主库、B 分片、Mac repo | 待启动 | 无 |  |
| WIN_DATA_CURATION_B | Windows | Codex app | B 尾差 B 分片 + C 队列 B 分片 | `data/gaokao_staging/**` 中 B 分片工作文件，`output/review_patches/curation_b/**`, `docs/dev/review_logs/curation_b.md` | 主库、A 分片、Mac repo | 待启动 | 无 |  |

---

## 3. 关键交接约定

### 3.1 数据库相关
- 只有 `WIN_DB_CORE` 可以写 `data/local_edu_tool/local_edu.sqlite3`
- `WIN_DATA_CURATION_A/B` 只能提交：
  - `patch.csv`
  - `audit.md`
  - `unresolved.csv`

### 3.2 应用相关
- Mac 侧在 DB RC 冻结前，不依赖尚未冻结的正式数据库结构
- 如果 Mac 侧需要新增 DB 字段或状态码，先写“变更请求”，由 `WIN_DB_CORE` 评估

### 3.3 文档相关
- 每个流只改自己的：
  - `WORKSTREAM` 文档
  - `review log`
  - `sync board row`
- 不得顺手改别的流的说明

---

## 4. 分片规则

### B 任务 `recruit_site` 尾差
- 首选 `college_id` 奇偶分片
- A 处理奇数
- B 处理偶数

### C 任务章程复核
- 同样使用 `college_id` 奇偶分片
- A 处理奇数
- B 处理偶数

### 兜底
若原始文件没有可用整数 ID：
1. 按学校名称排序
2. 交替分片
3. 第 1 行给 A，第 2 行给 B，以此类推

---

## 5. 每次更新模板

请按以下格式追加到自己的日志段落：

```text
[UTC+08 YYYY-MM-DD HH:MM]
- 本次完成：
- 新产物：
- 影响别人的点：
- 当前阻塞：
- 下一步：
```

---

## 6. 各流日志

### MAC_MAINLINE
[UTC+08 2026-04-14 13:51]
- 本次完成：修正 Stage B 草稿摘要的一处误判；`buildVolunteerDraftBoundaryInsightCards()` 不再把“未命中明确省份规则”的草稿候选误判成“通用考生规则”，对应影响已同步到志愿草稿打印页和报表中心 `volunteer_draft_summary` 导出前摘要。
- 新产物：`apps/frontend/tests/volunteer-workbench.test.ts` 新增草稿缺规则回归；`apps/frontend/tests/report-insight-presenter.test.ts` 新增报表摘要分发回归；README 与 memory-bank 已同步本次进展。
- 影响别人的点：无数据库结构变更；仅收口前端摘要逻辑，可直接被后续 Stage B 更细规则提示继续复用。
- 当前阻塞：无。
- 下一步：继续把草稿侧“缺少明确规则”从通用提示细分到“缺省份 / 缺年份 / 缺批次 / 缺模式”层级，并评估是否需要在不改 schema 的前提下保留更多工作台规则提醒上下文。

[UTC+08 2026-04-14 13:57]
- 本次完成：把同一套 Stage B 草稿边界口径补到后端 `volunteer_draft_summary` Excel 导出；当前“导出前摘要 / 打印页 / Excel 边界概览”都已能显式区分“缺少明确规则”和“通用类别规则口径”。
- 新产物：`apps/backend/tests/test_recommendation_exporters.py` 新增导出 workbook 回归；`apps/backend/app/exporters/recommendations.py` 已补导出边界概览与明细边界说明。
- 影响别人的点：无 schema / 状态码 / 数据库变更；仅增强现有导出说明文本，可直接与后续 Stage B 规则细化共存。
- 当前阻塞：草稿快照里仍不保留工作台 `rule_alerts` 结构，因此当前只能提示“缺少明确规则”，还不能在草稿/导出链精确区分“缺省份 / 缺年份 / 缺批次 / 缺模式”。
- 下一步：评估是否能在不改数据库结构的前提下从现有草稿快照推导更多规则缺口；如果不能，再按约定写 `mac_db_contract_request.md` 提变更请求。

[UTC+08 2026-04-14 14:03]
- 本次完成：把工作台 `rule_alerts` 复用到草稿详情读取链；当前志愿草稿打印页、报表中心 `volunteer_draft_summary` 导出前摘要和 Excel“边界概览”都已能细分“缺少目标年份规则”等具体缺口，而不需要新增数据库字段。
- 新产物：`VolunteerDraftRead.rule_alerts`、草稿详情实时规则告警计算、前端草稿边界卡片对 `rule_alerts` 的支持，以及对应前后端定向回归。
- 影响别人的点：无数据库结构变更；推荐草稿详情接口响应新增 `rule_alerts` 字段，前端已同步消费，其他调用方若仅读旧字段不会受影响。
- 当前阻塞：当前这套细分缺口仍只下沉到志愿草稿链，尚未同步到推荐报告等其他输出面。
- 下一步：继续评估是否把这套规则缺口提示扩到推荐报告打印/导出链，或直接转向更多省份/年份组合下的具体差异解释。

[UTC+08 2026-04-15 09:57]
- 本次完成：把草稿级 `applicable_rules` 继续下沉到输出面；当前志愿草稿打印页与 `volunteer_draft_summary` Excel 导出都已新增“规则差异摘要”，可并排展示当前控制规则与兼容预览规则。
- 新产物：`VolunteerDraftRead.applicable_rules`、`VolunteerDraftPrintPage.vue` 规则差异摘要区、`volunteer_draft_summary` 的“规则差异摘要” worksheet，以及对应前后端定向回归。
- 影响别人的点：无数据库结构变更；推荐草稿详情接口响应新增 `applicable_rules` 字段，前端已同步消费，其他调用方若仅读旧字段不会受影响。
- 当前阻塞：当前这套规则差异摘要仍只落在志愿草稿打印/导出链，尚未同步到推荐报告等其他输出面。
- 下一步：优先评估是否把相同的规则差异摘要继续下沉到推荐报告打印/导出链；如果收益不高，再转向更多省份/年份组合下的具体差异解释。

[UTC+08 2026-04-15 10:01]
- 本次完成：把同一套“规则差异摘要”补到报表中心 `volunteer_draft_summary` 导出前摘要；当前报表中心、草稿打印页和 Excel 三个草稿输出面都已具备“规则差异摘要 + 边界概览”双层说明。
- 新产物：`apps/frontend/src/components/reports/reportInsightPresenter.ts` 对草稿摘要的规则卡片映射，以及 `apps/frontend/tests/report-insight-presenter.test.ts` 的“当前控制规则”回归。
- 影响别人的点：无数据库结构变更；仅增强前端导出前摘要分发层，复用已有草稿详情字段。
- 当前阻塞：推荐报告链当前没有同等稳定的规则差异底座，若继续向推荐报告扩展，需要先确认是否存在足够可靠的规则上下文，不宜直接推断。
- 下一步：优先评估推荐报告链是否有足够稳定的规则上下文可复用；如果没有，直接转向“更多省份/年份组合下的具体差异解释”。

[UTC+08 2026-04-15 10:08]
- 本次完成：继续补“更多省份/年份组合下的差异解释”第一步；当前工作台边界概览、草稿打印页、报表中心 `volunteer_draft_summary` 导出前摘要和 Excel“边界概览”都已新增“参考年份偏旧”提示，明细“边界说明”也会在录取样本最近年份与目标年份相差 2 年及以上时提醒“排序和解释偏保守”。
- 新产物：`volunteerWorkbench.ts` 的“参考年份偏旧”摘要/逐条说明、`apps/backend/app/exporters/recommendations.py` 的同口径导出文案，以及对应前后端定向回归。
- 影响别人的点：无数据库结构变更；仅复用现有 `reference_years_json` 和 `year` 快照字段，不影响其他调用方。
- 当前阻塞：当前仍缺“同校同专业跨省样本差异”的聚合级说明，用户还需要从逐条候选里自己感知省份差异。
- 下一步：优先评估是否把“同校同专业跨省参考差异”也收成全局摘要；如果现有结果面信息不够，再改做更多年份组合下的保守性提示细化。

[UTC+08 2026-04-15 10:16]
- 本次完成：把“参考年份偏旧”继续扩到推荐报告链；当前推荐结果输出前复核、推荐打印页、报表中心 `recommendation_summary` 导出前摘要和 Excel“风险概览”都已能在结果快照最近录取样本与目标年份相差 2 年及以上时给出全局提醒。
- 新产物：`reportInsightRecommendation.ts` 的推荐报告“参考年份偏旧”风险卡、`recommendation_summary` Excel“风险概览”同口径提示，以及 `target_year` 在推荐输出前复核/打印/报表摘要链的透传。
- 影响别人的点：无数据库结构变更；仅复用现有 `RecommendationHistoryItem.target_year` 和 `snapshot_json.reference_years`，不影响历史结果存储结构。
- 当前阻塞：现有推荐结果快照仍没有“跨省对照样本”上下文，暂时不适合硬做“同校同专业跨省差异”的聚合级提示。
- 下一步：优先评估是否能在不改 schema 的前提下，从现有结果集或历史对比链提炼“同校同专业跨省参考差异”摘要；如果不够稳，就继续补同省跨年份口径变化解释。

[UTC+08 2026-04-15 10:25]
- 本次完成：把“参考年份偏旧”继续下沉到推荐结果逐条说明；当前推荐结果页与推荐打印页单条“理由”已直接补出这条提示，不再只依赖全局风险卡。
- 新产物：`recommendationCopy.ts` 的共享 helper、`RecommendationSchemeResultsPanel.vue` / `RecommendationPrintPage.vue` 的逐条说明复用，以及 `recommendation-copy.test.ts`。
- 影响别人的点：无数据库结构变更；仅增强前端结果展示与打印说明，不影响推荐结果接口。
- 当前阻塞：现有多方案对照仍只展示新增/移除/分组变化，还没有“同省跨年份参考变化”的聚合级说明。
- 下一步：优先评估是否把多方案对照继续扩成“同省跨年份参考变化”摘要；如果收益不高，再转向其他 Stage B 聚合级差异解释。

[UTC+08 2026-04-15 10:29]
- 本次完成：把多方案对照继续扩成“参考年份变化”摘要；当前推荐页单方案对比卡片与批量对照表都已能汇总同一院校/专业在不同方案里的最近录取样本年份变化。
- 新产物：`recommendationComparison.ts`、单方案对比区的“参考年份变化”摘要卡、批量对照表的“参考年变化”列，以及 `recommendation-comparison.test.ts`。
- 影响别人的点：无数据库结构变更；仅增强前端推荐方案对照解释，不影响推荐结果接口与历史存储。
- 当前阻塞：当前仍未把“参考年份变化如何影响冲稳保分组”解释到更细层级，用户还需要结合新增/移除/分组变化自行判断影响。
- 下一步：优先评估是否把“参考年份变化导致的分组变化”继续收成一句聚合解释；如果收益不高，再转向报表中心或其他输出面的推荐对照摘要。

[UTC+08 2026-04-15 10:43]
- 本次完成：把多方案对照里的“参考年份变化”继续细化到“年变伴随分组变”；当前单方案对比说明会直接提示有多少条参考年份变化同时伴随冲稳保分组调整，批量对照表也能直接看这项计数。
- 新产物：`recommendationComparison.ts` 的 `groupShiftCount` 统计、`RecommendationSchemeResultsPanel.vue` 的聚合说明文案与“年变伴随分组变”列，以及 `recommendation-comparison.test.ts` 新增的两类回归。
- 影响别人的点：无数据库结构变更；仅增强前端推荐方案对照解释，不影响推荐结果接口、历史快照和导出格式。
- 当前阻塞：这套聚合解释还只在推荐页对照区可见，尚未同步到报表中心的推荐对照输出面。
- 下一步：优先评估是否把同样的对照摘要同步到报表中心推荐输出；如果收益一般，再转向更多省份/模式下的聚合差异解释。

### WIN_DB_CORE
- 待填写

### WIN_DATA_CURATION_A
- 待填写

### WIN_DATA_CURATION_B
- 待填写
