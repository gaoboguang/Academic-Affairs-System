# 同步板（2026-04-16）

> 使用规则：
> 1. 每个会话只改自己的那一行和自己的日志段落  
> 2. 时间统一写 `UTC+08`  
> 3. 不要整页重写  
> 4. 影响别的线程的事项，先写 `影响说明` 再继续  
> 5. Windows 侧所有数据库状态，统一以 `DB RC1` / `DB_CORE` handoff 为准

---

## 1. 当前全局状态

- 总策略：**Mac 主应用主线；Windows 数据库权威线**
- 当前数据库基线：**`DB RC1`**
- 当前 Windows 唯一高优先级：**子任务 C 收口**
- 当前 Mac 唯一高优先级：**高考数据只读驾驶舱 + Stage B 输出解释统一**
- 主库写权限：**仅 `WIN_DB_CORE`**
- Mac 不依赖现场工作库瞬时状态
- A/B 只提交 patch / unresolved / audit

---

## 2. 当前版本基线（4.16 最新口径）

### Windows / `DB RC1`
- 生成口径时间：`2026-04-16 10:57`
- `gaokao_college_total = 3344`
- `gaokao_college_with_recruit_site = 1813`
- `gaokao_college_with_chapter_url = 172`
- `chapter_rule_fallback_url_filled = 1805`
- `duplicate_group_total = 4`
- `same_name_cross_site_group_total = 2`

### 当前判断
- A：已完成
- B：主体完成，剩余尾差与边界问题
- C：中后段推进中，仍是唯一明显瓶颈

---

## 3. 工作流总表

| Stream | Device | Surface | 当前目标 | Owned paths | No-touch | 当前状态 | 阻塞 | 最后更新 |
|---|---|---|---|---|---|---|---|---|
| `MAC_MAINLINE` | Mac | Codex CLI | 只读驾驶舱 + Stage B 解释链统一 + 安全边界修复 | `apps/frontend/**`, `apps/backend/app/api/**`, `apps/backend/app/services/**`, `apps/backend/tests/**`, `tests/e2e/**`, `docs/**`, `memory-bank/**`, `README.md` | Windows 主库、Windows patch 合并、Windows review 队列 | 进行中：P0/P1/P2/P4/P5 已收一轮；P1 本轮把高考驾驶舱继续推进到“学校队列可排优先级 + 组卡可直接做差异对照”的只读排障台 | 无 | `2026-04-19 15:51 UTC+08` |
| `WIN_DB_CORE` | Windows | Codex app | `DB RC1` handoff / merge gate / C 收口合并 / `RC2` 准备 | `data/local_edu_tool/**`, `apps/backend/alembic/**`, `apps/backend/app/importers/**`, `apps/backend/scripts/**`, `output/db_release/**`, `docs/dev/**` 中数据库交接相关文档 | Mac 主应用复杂页面；A/B 分片原始工作文件 | 待启动 | 无 |  |
| `WIN_DATA_CURATION_A` | Windows | Codex app | C 队列 A 分片 + 少量 `recruit_site` 尾差 A 分片 | `output/review_patches/curation_a/**`, `docs/dev/review_logs/curation_a.md`, A 分片临时工作文件 | 主库、B 分片、Mac repo | 待启动 | 无 |  |
| `WIN_DATA_CURATION_B` | Windows | Codex app | C 队列 B 分片 + 少量 `recruit_site` 尾差 B 分片 | `output/review_patches/curation_b/**`, `docs/dev/review_logs/curation_b.md`, B 分片临时工作文件 | 主库、A 分片、Mac repo | 待启动 | 无 |  |

---

## 4. 关键交接约定

### 4.1 数据库相关
- 只有 `WIN_DB_CORE` 可以写：
  - `data/local_edu_tool/local_edu.sqlite3`
- `WIN_DATA_CURATION_A/B` 只能提交：
  - `patch.csv`
  - `audit.md`
  - `unresolved.csv`
  - 可选 `evidence/`

### 4.2 应用相关
- Mac 侧优先做**只读**数据驾驶舱
- Mac 侧在没有正式 handoff 前，不依赖未冻结 schema
- 如果 Mac 侧真的需要新增字段或状态码，先发 `DB contract request`

### 4.3 文档相关
- 每个流只改自己的：
  - `WORKSTREAM` 文档
  - `review log`
  - `sync board row`
- 不得顺手改别的流的说明

---

## 5. 分片规则

### 默认规则
优先使用整数 `college_id`

- A 处理奇数
- B 处理偶数

### 若没有整数 `college_id`
按顺序退化：

1. `college_code` 最后一位奇偶
2. 学校名称排序后的奇偶行号

### 原则
必须是**确定性分片**，别人能复现你的筛选逻辑。

---

## 6. 本轮里程碑

### M1：Windows 基线清晰
- `WIN_DB_CORE` 完成 `DB RC1` handoff / summary / merge gate 说明

### M2：A/B 新一轮 C 收口
- A/B 各自完成一轮新的 chapter patch / unresolved / audit

### M3：Mac 驾驶舱第一版
- 数据总览页
- 数据审阅页
- 推荐解释证据页
- 山东首期数据监控页

### M4：`RC2` 预备
- 继续清理 duplicate / same-name
- 形成 `RC2` 条件评估

---

## 7. 每次更新模板

请按以下格式追加到自己的日志段落：

```text
[UTC+08 YYYY-MM-DD HH:MM]
- 本次完成：
- 新产物：
- 影响说明：
- 当前阻塞：
- 下一步：
```

---

## 8. 各流日志

### `MAC_MAINLINE`
[UTC+08 2026-04-19 15:51]
- 本次完成：继续推进 P1“高考数据只读驾驶舱”；把重复组 / 同名组继续升级成可直接对照的差异视图，`GET /api/gaokao/review-summary` 现返回组级 `comparison_fields` 和组成员的 `official_site / recruit_site / effective_chapter_url / updated_at` 等对照字段；审阅页组卡现在能直接看到省份、院校代码、官网、招生网、章程入口的差异摘要，并在卡内表格里逐校对比。
- 新产物：`apps/backend/app/schemas/gaokao.py`、`apps/backend/app/services/gaokao.py`、`apps/backend/tests/test_gaokao_api.py`、`apps/frontend/src/pages/GaokaoDataPage.vue`。
- 影响说明：高考驾驶舱从“组级问题能排队”进一步推进到“组卡内即可完成首轮差异对照”；负责人现在在重复组 / 同名组卡里就能看出代码、省份、官网、招生网、章程入口哪里不同、哪里缺失，不必先逐个点进证据页再人工比对；仍是只读实现，无后端写入和 DB 结构改动。
- 当前阻塞：这轮解决的是组内对照展示，不涉及真正的裁决写回、批量标记或与 Windows review 队列联动；如果继续推进大步，下一步应考虑把来源标题/来源链接差异和更细的字段冲突提示继续沉到同一视图。
- 下一步：继续按开发文档推进 P1/P3，优先评估是否补组级来源差异与冲突摘要，或者回到 Stage B 解释链继续收口剩余输出面。

[UTC+08 2026-04-19 15:38]
- 本次完成：继续推进 P1“高考数据只读驾驶舱”；把重复组 / 同名组升级成组级优先级视图，`GET /api/gaokao/review-summary` 现返回 `priority_groups` 和组级 `priority_* / suggested_action` 信息；审阅页新增“建议先处理的组”区，重复组 / 同名组列表也会显示组级优先级、原因和建议动作。
- 新产物：`apps/backend/app/schemas/gaokao.py`、`apps/backend/app/services/gaokao.py`、`apps/backend/tests/test_gaokao_api.py`、`apps/frontend/src/pages/GaokaoDataPage.vue`。
- 影响说明：高考驾驶舱从“学校队列能排序”进一步推进到“组级问题也能排队处理”；负责人现在不仅能先找最急的学校，还能直接看到哪一个重复组或同名组最值得优先裁决，并从该组卡片一键打开成员证据链；仍是只读实现，无后端写入和 DB 结构改动。
- 当前阻塞：这轮解决的是组级排序和行动提示，不涉及真正的裁决写回或与 Windows review 队列的双向联动；如果继续推进大步，下一步应考虑把组级优先级与更细的差异说明、来源对照继续下沉。
- 下一步：继续按开发文档推进 P1/P3，优先评估是否补组级差异对照与更细的来源说明，或者回到 Stage B 解释链继续收口剩余输出面。

[UTC+08 2026-04-19 14:43]
- 本次完成：继续推进 P1“高考数据只读驾驶舱”；审阅页新增“高优先 / 缺章程 / 缺招生网 / 重复或同名组 / 仍未解决”快速筛选，并把学校明细改成后端优先级分层后默认按优先级排序；`GET /api/gaokao/review-summary` 现支持 `focus` / `sort`，返回 `active_focus`、`active_sort`、`queue_total`、`quick_filters` 和逐校 `priority_*` 信息。
- 新产物：`apps/backend/app/api/routes/gaokao.py`、`apps/backend/app/schemas/gaokao.py`、`apps/backend/app/services/gaokao.py`、`apps/backend/tests/test_gaokao_api.py`、`apps/frontend/src/pages/GaokaoDataPage.vue`。
- 影响说明：高考审阅页从“能搜问题”进一步推进到“能直接判断先看什么”；负责人现在可以先看高优先学校，再一键切到缺章程、缺招生网或重复/同名组队列，不需要自己凭表格逐行判断优先级；仍是只读实现，无后端写入和 DB 结构改动。
- 当前阻塞：这轮解决的是审阅队列的排序和筛选，不涉及真正的人工裁决或写回链路；如果继续推进大步，下一步应考虑把“优先级原因”进一步沉到组级视图或补更细的审阅排序规则。
- 下一步：继续按开发文档推进 P1/P3，优先评估是否把重复/同名组也接成同一套优先级视图，或者回到 Stage B 解释链继续收口剩余输出面。

[UTC+08 2026-04-18 00:59]
- 本次完成：继续推进 P1“高考数据只读驾驶舱”；审阅页新增关键字检索，`GET /api/gaokao/review-summary` 现在支持 `keyword` 过滤并返回命中总数与高亮提示；山东监控页补了“已接入板块 / 待补齐板块”汇总和优先缺口提示，页面可以直接告诉使用者先补哪些山东首期能力。
- 新产物：`apps/backend/app/api/routes/gaokao.py`、`apps/backend/app/schemas/gaokao.py`、`apps/backend/app/services/gaokao.py`、`apps/backend/tests/test_gaokao_api.py`、`apps/frontend/src/pages/GaokaoDataPage.vue`。
- 影响说明：高考驾驶舱从“能钻取证据链”继续前进到“能快速搜问题并判断补数优先级”；负责人现在可以在审阅页直接按学校名 / code / 省份等关键字缩小范围，并在山东监控页看到现阶段最值得优先补齐的板块；无后端写入和 DB 结构改动。
- 当前阻塞：这轮补齐的是只读搜索和 gap 提示，不涉及 DB RC1 写回或更深的审阅动作；如果还要继续跃进，需要进一步强化审阅结果排序、优先级分层或把更多解释链接到这套只读驾驶舱。
- 下一步：继续按开发文档推进 P1/P3，优先评估是否补高考审阅页的优先级排序/快速筛选，或者回到 Stage B 解释链继续收口剩余输出面。

[UTC+08 2026-04-18 00:51]
- 本次完成：继续推进 P1“高考数据只读驾驶舱”；新增 `GET /api/gaokao/college-options` 只读搜索接口，并把驾驶舱里的证据页升级成可按学校名 / code / ID 搜索、可从审阅表一键跳转、可从重复组 / 同名组成员直接点开单校证据链的排障入口，不再要求使用者先知道学校 ID。
- 新产物：`apps/backend/app/api/routes/gaokao.py`、`apps/backend/app/schemas/gaokao.py`、`apps/backend/app/services/gaokao.py`、`apps/backend/tests/test_gaokao_api.py`、`apps/frontend/src/pages/GaokaoDataPage.vue`、`apps/frontend/src/utils/gaokaoEvidence.ts`、`apps/frontend/tests/gaokao-evidence.test.ts`。
- 影响说明：高考驾驶舱从“只读看板”前进到“可钻取排障台”；项目负责人现在可以直接从待审阅学校、重复组或名称搜索进入证据页，看官网/招生网/章程/来源链接/审阅状态，不用切数据库工具，也不用记学校主键；无后端写入和 DB 结构改动。
- 当前阻塞：单校证据链和审阅 drill-down 已打通，但如果后续要继续提升驾驶舱演示效果，下一步应补更强的 review 搜索筛选、组内排序说明或山东监控的优先缺口提示。
- 下一步：继续按开发文档推进 P1/P3，优先评估是否补高考审阅页的关键字检索与更直观的 gap 提示，或者回到 Stage B 解释链把剩余输出面继续统一。

[UTC+08 2026-04-18 00:39]
- 本次完成：继续推进 P3“分析与导出模板统一”；把推荐输出提醒也接到统一 resolver，`useRecommendationSchemeExport` 在打印/导出前会优先复用当前学生的历史方案列表，找到最近对照方案后把 compare context 一并传给 warning builder，推荐输出前提醒不再只看当前方案单点风险。
- 新产物：`apps/frontend/src/components/recommendations/recommendationOutputGuards.ts`、`apps/frontend/src/components/recommendations/useRecommendationSchemeExport.ts`、`apps/frontend/src/components/recommendations/useRecommendationSchemeComparison.ts`、`apps/frontend/tests/recommendation-output-guards.test.ts`。
- 影响说明：推荐报告导出/打印前的确认弹窗现在在有历史方案和对照结果时，会把“历史方案差异 / 跨省口径差异 / 参考年份变化”等提醒也纳入 warning 文案；当前推荐结果卡片口径不变，无后端或 DB 改动。
- 当前阻塞：这次统一到了推荐输出提醒，但仍需继续盘点是否还有其他推荐链输出面未走同一 resolver/insight builder。
- 下一步：继续按开发文档推进 P3，优先检查推荐中心剩余按钮流和其他输出 guard 是否还存在页面层或 hook 层重复拼装解释参数，再收下一小闭环。

[UTC+08 2026-04-18 00:17]
- 本次完成：继续推进 P3“分析与导出模板统一”；把推荐摘要上下文 resolver 统一下沉到 `reportInsightRecommendation`，`ReportsPage` 和 `RecommendationPrintPage` 现在共用同一套 `currentOption / compareScheme / compareOption` 拼装逻辑，不再各自查找最近历史方案并组装 compare option。
- 新产物：`apps/frontend/src/components/reports/reportInsightRecommendation.ts`、`apps/frontend/src/pages/ReportsPage.vue`、`apps/frontend/src/pages/RecommendationPrintPage.vue`、`apps/frontend/tests/report-insight-recommendation.test.ts`。
- 影响说明：当前推荐报告的卡片口径与分组不变，但推荐报表中心、打印页和后续输出提醒现在更容易走同一解释上下文链路；这次主要消除“当前方案/历史方案解释参数”重复拼装，后续如果再扩展推荐解释字段，不需要两处页面分别改。
- 当前阻塞：统一的是推荐解释上下文入口，不是新增 live rule 或结果字段；若下一步继续做 P3，应继续盘点是否还有其他推荐输出面仍在页面层重复拼装解释参数。
- 下一步：继续按开发文档推进 P3，优先检查推荐导出提醒、打印页和报表中心之外是否还有未走统一 resolver 的解释链入口，再收下一小闭环。

[UTC+08 2026-04-18 00:08]
- 本次完成：继续推进 P3“分析与导出模板统一”；把推荐报告和志愿草稿的摘要分组入口统一下沉到 `reportInsightPresenter`，`ReportsPage`、`RecommendationPrintPage`、`VolunteerDraftPrintPage` 现在共用同一套“按报表类型决定如何分组”的接线，不再各自判断推荐/草稿分组逻辑。
- 新产物：`apps/frontend/src/components/reports/reportInsightPresenter.ts`、`apps/frontend/src/pages/ReportsPage.vue`、`apps/frontend/src/pages/RecommendationPrintPage.vue`、`apps/frontend/src/pages/VolunteerDraftPrintPage.vue`、`apps/frontend/tests/report-insight-presenter.test.ts`。
- 影响说明：当前功能口径不变，仍保持推荐报告的“历史对照摘要 / 边界概览 / 风险概览”和志愿草稿的“规则差异摘要 / 边界概览 / 风险概览”；这次主要消除页面层重复接线，后续继续收口摘要结构时不必分别改报表中心和两个打印页。
- 当前阻塞：统一的是摘要分组入口，不是新增数据口径；若下一步继续做 P3，应继续盘点是否还有其他打印/导出面仍未走统一 presenter/summary 入口。
- 下一步：继续按开发文档推进 P3，优先检查剩余打印页和导出入口是否还有类似页面层重复组装逻辑，再收下一小闭环。

[UTC+08 2026-04-18 00:01]
- 本次完成：继续推进 P3“分析与导出模板统一”；`growth_summary` 和 `teacher_workload` 两类导出现在都会在保留原主 sheet 的前提下追加统一的 `摘要概览` sheet，成长档案与工作量 Excel 也开始和其他分析/运营报表共用摘要层入口。
- 新产物：`apps/backend/app/exporters/archive.py`、`apps/backend/app/exporters/workload.py`、`apps/backend/tests/test_archive_and_system.py`、`apps/backend/tests/test_workload_workflow.py`。
- 影响说明：成长档案导出会显示“成长档案整体状态 / 高频成长类型 / 最近一条记录”，工作量导出会显示“工作量整体状态 / 工作量最高教师”等摘要卡；原主 sheet 名称和下载入口不变，仅在末尾追加 `摘要概览`；无 DB 改动。
- 当前阻塞：分析与运营导出的统一摘要入口已进一步补齐，但仍需继续核对是否还有其他导出面未接同一摘要结构。
- 下一步：继续按开发文档推进 P3，优先盘点剩余打印/导出面是否还存在未统一的摘要入口，再决定是否继续补共享结构。

[UTC+08 2026-04-17 23:55]
- 本次完成：继续推进 P3“分析与导出模板统一”；`evaluation_summary` 和 `adviser_quant_summary` 两类导出现在都会在保留原主 sheet 的前提下追加统一的 `摘要概览` sheet，评教/量化 Excel 也开始和学生/班级/年级/教师分析导出使用同一摘要入口。
- 新产物：`apps/backend/app/exporters/reports.py`、`apps/backend/tests/test_evaluation_quant_workflow.py`。
- 影响说明：评教汇总和班主任量化导出不再只有明细与总览主 sheet，Excel 里现在也有可先看结论的摘要层；现有 sheet 名称和导出主结构不变，仅在末尾追加 `摘要概览`；无 DB 改动。
- 当前阻塞：`growth_summary` 和 `teacher_workload` 仍未补同类 `摘要概览` sheet；若继续做统一导出入口，可以沿同一模式补齐。
- 下一步：继续按开发文档推进 P3，优先补 `growth_summary` / `teacher_workload` 的统一摘要入口，或者回头收口尚未共用结构的打印/导出面。

[UTC+08 2026-04-17 23:50]
- 本次完成：继续推进 Stage B 解释链统一；`recommendation_summary` 的 Excel 导出现在也新增 `导出前摘要` sheet，复用既有“历史对照摘要 / 边界概览 / 风险概览”分组，把推荐报告的 Excel、打印页和报表中心导出前摘要进一步拉齐。
- 新产物：`apps/backend/app/exporters/recommendations.py`、`apps/backend/tests/test_recommendation_exporters.py`。
- 影响说明：推荐报告导出不再只有 `风险概览` 一个结构化 sheet，Excel 里现在也有和前端摘要顺序一致的统一入口；原有 `风险概览` sheet 保持不变，兼容既有导出读取口径；无 DB 改动。
- 当前阻塞：推荐生成链本身仍未接 province rule 命中上下文，当前统一的是“已有摘要结构”，不是强行补不存在的 live 规则字段。
- 下一步：继续按开发文档推进 P2/P3，优先看还有哪些打印/导出输出面尚未共享同一摘要入口，或评估是否存在无需改 schema 的 recommendation rule context 只读补充方案。

[UTC+08 2026-04-17 23:44]
- 本次完成：继续推进 Stage B 解释链统一；`volunteer_draft_summary` 的 Excel 导出现在新增 `导出前摘要` sheet，把规则差异、边界和风险按“规则差异摘要 / 边界概览 / 风险概览”统一落盘，和报表中心、打印页的摘要分组顺序对齐。
- 新产物：`apps/backend/app/exporters/recommendations.py`、`apps/backend/tests/test_recommendation_exporters.py`。
- 影响说明：志愿草稿导出不再只有分散的“边界概览 / 规则差异摘要”明细 sheet，Excel 里现在也有统一入口可先快速看摘要层次；无 DB 改动。
- 当前阻塞：`recommendation_summary` 生成链当前仍未直接暴露 province rule 命中上下文，暂不硬接不存在的 live 规则字段，避免造出假口径。
- 下一步：继续按开发文档推进 P2，优先评估是否以最小只读合同补 `recommendation_summary` 的规则上下文，或继续收口其他导出链的共用摘要结构。

[UTC+08 2026-04-17 23:39]
- 本次完成：继续推进 Stage B 解释链统一；`volunteer_draft_summary` 的摘要卡组装已下沉到共享 helper，`VolunteerDraftPrintPage` 现在和报表中心共用同一套志愿草稿摘要卡与分组逻辑，打印页也按“规则差异摘要 / 边界概览 / 风险概览”顺序展示。
- 新产物：`apps/frontend/src/components/reports/reportInsightVolunteerDraft.ts`、`apps/frontend/src/components/reports/reportInsightPresenter.ts`、`apps/frontend/src/pages/VolunteerDraftPrintPage.vue`、`apps/frontend/tests/report-insight-volunteer-draft.test.ts`。
- 影响说明：志愿草稿打印页不再单独维护一套规则卡渲染逻辑，和报表中心的 live 规则差异 / 边界 / 风险分组口径已统一；纯前端改动，无 DB 改动。
- 当前阻塞：`recommendation_summary` 生成链当前仍未直接暴露 province rule 命中上下文，暂不硬接不存在的 live 规则字段，避免造出假口径。
- 下一步：继续按开发文档推进 P2，优先评估是否以最小只读合同补 `recommendation_summary` 的规则上下文，或继续收口其他打印/导出摘要链的共用结构。

[UTC+08 2026-04-17 23:29]
- 本次完成：继续推进 Stage B 解释链统一；报表中心里的 `volunteer_draft_summary` 导出前摘要现在会把草稿规则卡、缺规则/通用规则/兼容模式等 live 规则差异，以及跨省/跨年份/选科待核对等边界信息按“规则差异摘要 / 边界概览 / 风险概览”分组展示，阅读顺序与推荐摘要链更一致。
- 新产物：`apps/frontend/src/components/reports/reportInsightVolunteerDraft.ts`、`apps/frontend/src/components/reports/reportInsights.ts`、`apps/frontend/src/pages/ReportsPage.vue`、`apps/frontend/tests/report-insight-volunteer-draft.test.ts`。
- 影响说明：`volunteer_draft_summary` 在报表中心不再把 live 规则差异和普通边界卡混排，和工作台/打印页的“先看规则、再看边界、最后看风险”顺序更一致；纯前端改动，无 DB 改动。
- 当前阻塞：`recommendation_summary` 生成链当前仍未直接暴露 province rule 命中上下文，暂不硬接不存在的 live 规则字段，避免造出假口径。
- 下一步：继续按开发文档推进 P2，优先评估是否以最小只读合同把 `recommendation_summary` 的规则上下文补齐，或继续收口其他打印/导出摘要链的同口径分组。

[UTC+08 2026-04-17 23:21]
- 本次完成：继续推进 P3“分析与导出模板统一”；后端 `student_analysis`、`class_analysis`、`grade_summary`、`teacher_analysis` 四类 Excel 导出现在都会在保留原主 sheet 的前提下追加统一的 `摘要概览` sheet，摘要文案与前端打印/报表分析 helper 对齐。
- 新产物：`apps/backend/app/exporters/reports.py`、`apps/backend/tests/test_exam_workflow.py`、`apps/backend/tests/test_grade_analytics_and_student_attachments.py`。
- 影响说明：学生/班级/年级/教师四条分析导出链现在都有同口径的“整体状态 / 优势项 / 关注项”摘要，不需要重新发明一套导出口径；原有主 sheet 名称与顺序保持兼容，仅在末尾追加 `摘要概览`；无 DB 改动。
- 当前阻塞：无新增阻塞。
- 下一步：继续按开发文档收口 P2/P3，优先看 live 规则差异是否还要继续并入统一摘要结构，或继续补分析打印/导出共用口径的剩余尾项。

[UTC+08 2026-04-17 23:11]
- 本次完成：落地考试板块 `V1`。考试“科目配置”现已改成整张学科清单勾选，默认支持 `语文/数学/英语/日语/俄语 = 150`、`政治/历史/地理/物理/化学/生物 = 100`，并允许逐科自定义；后端学科列表读取时也会自愈补齐内置 `日语/俄语`。
- 新产物：`apps/frontend/src/pages/ExamsPage.vue`、`apps/frontend/src/components/exams/examSubjectConfig.ts`、`apps/frontend/tests/exam-subject-config.test.ts`、`apps/backend/app/services/bootstrap.py`、`apps/backend/app/services/base_data.py`、`apps/backend/tests/test_api_m1.py`。
- 影响说明：考试创建后的科目配置路径从“逐行新增”收敛为“直接勾选”，常见学科默认满分不再靠人工反复输入；老库缺少 `日语/俄语` 时无需手工补基础学科；无 DB 结构改动。
- 当前阻塞：无新增阻塞；“按学生分科限制科目”仍缺正式学生选科合同，当前先不越界实现。
- 下一步：如继续考试线，先评估学生选科字段合同；否则回到 P2/P3 继续收口 live 规则差异或分析导出摘要一致性。

[UTC+08 2026-04-17 22:55]
- 本次完成：继续推进 `recommendation_summary` 的 Stage B 解释链，把推荐报告摘要进一步细化成“历史对照摘要 / 边界概览 / 风险概览”三段；前端报表中心与打印页、后端 Excel“风险概览”sheet 的“分组”列现已统一同一口径。
- 新产物：`apps/frontend/src/components/reports/reportInsightRecommendation.ts`、`apps/backend/app/exporters/recommendations.py`、`apps/frontend/tests/report-insight-recommendation.test.ts`、`apps/backend/tests/test_recommendation_exporters.py`、`apps/backend/tests/test_recommendation_workflow.py`。
- 影响说明：模拟结果、样本不足、缺少位次口径、需人工复核、参考年份偏旧等“边界提示”不再和偏好风险混排，推荐报告的历史差异 / 边界条件 / 风险提示层次更清楚；无 DB 改动。
- 当前阻塞：无新增阻塞。
- 下一步：继续推进 P2，优先把 live 规则差异补进同一三段结构，并保持前后端测试和 memory-bank 同步。

[UTC+08 2026-04-17 22:45]
- 本次完成：继续收口 `recommendation_summary` 的 Stage B 解释链；报表中心导出前摘要与推荐打印页现统一分成“历史对照摘要 / 风险概览”两组，Excel“风险概览”sheet 也新增“分组”列；同时顺手修正 `test_recommendation_workflow.py` 一条已过时的草稿边界断言。
- 新产物：`apps/frontend/src/components/reports/reportInsightTypes.ts`、`apps/frontend/src/components/reports/reportInsightRecommendation.ts`、`apps/frontend/src/components/reports/ReportInsightPanel.vue`、`apps/frontend/src/pages/ReportsPage.vue`、`apps/frontend/src/pages/RecommendationPrintPage.vue`、`apps/backend/app/exporters/recommendations.py`、相关前后端测试。
- 影响说明：推荐报告的历史对照解释不再和普通风险卡混排，报表中心 / 打印 / Excel 三条输出面对“历史方案差异、跨省差异、同省跨年份差异、参考年份偏旧”的层次更一致；无 DB 改动。
- 当前阻塞：无新增阻塞。
- 下一步：继续推进 P2，优先把 live 规则差异或更多跨省/跨年份聚合说明补到同一分组结构，并保持测试与 memory-bank 同步。

[UTC+08 2026-04-17 21:56]
- 本次完成：继续 Stage B 口径收口：草稿导出风险摘要新增“同省跨年份差异”与“跨省口径差异”独立卡片，并补齐 `apps/backend/tests/test_recommendation_exporters.py` 对应回归断言（含既有细项文案校验同步）。
- 新产物：`apps/backend/app/exporters/recommendations.py`、`apps/backend/tests/test_recommendation_exporters.py`。
- 影响说明：推荐导出风控汇总与工作台/报表口径更一致，跨省与同省跨年差异在草稿导出上独立可见，可避免口径误读；无 DB 改动。
- 当前阻塞：无新增阻塞；后续若继续补齐“参考年份偏旧”提示，需要确认现有接口字段是否已包含所有历史方案场景。
- 下一步：继续推进 Stage B 第三优先任务（打印页/Excel/报表中心摘要的统一），并同步更新本轮文档/测试说明（在 02_SYNC_BOARD 与 memory-bank 级说明）。

[UTC+08 2026-04-17 21:42]
- 本次完成：把志愿草稿导出边界概览补齐“跨省口径差异”聚合逻辑，并补相应后端导出测试用例（草稿 rows 覆盖广东+四川）。
- 新产物：`apps/backend/app/exporters/recommendations.py`、`apps/backend/tests/test_recommendation_exporters.py`（后者包含“跨省口径差异”边界聚合断言）。
- 影响说明：报表导出层的口径和页面/草稿打印边界概览收敛，减少“跨省场景”口径理解差异；草稿导出风险表已更完整地反映多省混合样本。
- 当前阻塞：当前 Python 测试环境缺少 `pytest` 可执行，`test_recommendation_exporters.py` 无法在本机直接运行；如需可补建会话本地依赖后再补跑通。
- 下一步：继续把 Stage B 同省跨年份/参考年份偏旧提示同步到“推荐方案”报表出口（非草稿线）并补前端展示一致性用例。

[UTC+08 2026-04-17 21:39]
- 本次完成：补齐工作台边界概览中的“跨省口径差异”聚合卡，并补齐前端对应最小回归测试（预览/草稿两侧各一例）。
- 新产物：无新增独立文件；增量更新 `apps/frontend/src/components/recommendations/volunteerWorkbenchInsights.ts` 及 `apps/frontend/tests/volunteer-workbench.test.ts`（补测 2 个边界卡场景）。
- 影响说明：跨省场景下边界提示从候选级说明下沉到聚合卡展示，避免页面/草稿/报表链口径分裂；同类场景可沿同一结构扩展。
- 当前阻塞：无硬阻塞；如要新增数据库字段仍需由 `WIN_DB_CORE` 发起 schema 升级评估。
- 下一步：继续 Stage B 第三点“参考年份偏旧/跨省/跨年份/规则差异摘要”收口到报表中心和打印页的统一字段，保持不触碰 schema。

[UTC+08 2026-04-17 21:33]
- 本次完成：继续把 Stage B 工作台预览的 live 规则缺口解释改成按候选数汇总；当前工作台“边界概览”里的 `missing_rule_*` 和“已回退到通用考生规则”会直接显示受影响候选数量，不再只停留在泛化提示。
- 新产物：无新增独立文件；增量更新 `apps/frontend/src/components/recommendations/volunteerWorkbenchInsights.ts` 及对应前端测试。
- 影响说明：工作台页面和草稿打印/报表摘要对这类 live 规则差异的表达更一致；同时保留“无边界卡时显示 stable”的场景，避免把稳定预览误打成回退状态。
- 当前阻塞：无硬阻塞；若后续要把“跨省口径”或更细 live 规则差异直接做成聚合结构，仍需先确认现有快照/preview 是否已稳定提供可用字段。
- 下一步：继续补更多省份/年份差异解释，优先看“跨省口径”或“已维护其他模式/批次/类别”的结构化聚合说明，而不是继续堆泛化提示。

[UTC+08 2026-04-17 21:24]
- 本次完成：继续把 Stage B 省份/年份差异解释下沉到工作台/草稿聚合层；当前工作台“边界概览”、草稿打印页、报表中心 `volunteer_draft_summary` 导出前摘要和 Excel“边界概览”都会显式汇总“类别专用规则口径”和“跨年份参考样本”。
- 新产物：无新增独立文件；增量更新 `apps/frontend/src/components/recommendations/volunteerWorkbenchInsights.ts`、`apps/backend/app/exporters/recommendations.py` 及对应前后端测试。
- 影响说明：同省同年不同类别、同省跨年份样本并存的差异不再只散落在候选级单条说明里；工作台、草稿打印/报表摘要和 Excel 已按同一聚合口径展示。
- 当前阻塞：无硬阻塞；若后续要把跨省口径或更多 live 规则差异直接下沉到聚合输出面，仍需先等 Windows `DB RC1` / contract handoff。
- 下一步：继续补更多省份/年份差异解释，优先看“跨省口径”和更细 live 规则差异应下沉到哪些聚合摘要，而不是只停留在候选级说明。

[UTC+08 2026-04-16 17:20]
- 本次完成：P0 安全边界补齐（恢复路径校验、重复路由唯一性锁定）；高考数据只读驾驶舱第一版（`/gaokao-data` + 5 个只读接口）；推荐打印/报表中心解释入参统一；`grade_summary` / `teacher_analysis` 导出摘要统一。
- 新产物：`apps/backend/app/services/gaokao.py`、`apps/backend/app/api/routes/gaokao.py`、`apps/backend/app/schemas/gaokao.py`、`apps/backend/tests/test_gaokao_api.py`、`apps/frontend/src/pages/GaokaoDataPage.vue`、`apps/frontend/tests/report-insight-recommendation.test.ts`。
- 影响说明：高考驾驶舱当前遵循“只读 + live table 优先 + fallback 到 sync board/现有应用模型”，未改 DB schema；`grade_summary` Excel 第一张 sheet 已变为“年级概况”。
- 当前阻塞：无硬阻塞；若后续要接更多 `gaokao_*` 字段或状态码，需先等 Windows `DB RC1` / contract handoff。
- 下一步：继续看分析/导出模板剩余尾项与 Stage B 更多省份/年份差异解释；最后只补本流相关文档和测试说明，不碰其他流。

[UTC+08 2026-04-16 17:47]
- 本次完成：继续完成高复杂模块小步拆分；Stage B 工作台边界/规则摘要拆到 `volunteerWorkbenchInsights.ts`，评教批次统计拆到 `_evaluation_batch_stats.py`，工作量逐教师计算拆到 `_workload_calculation.py`；同步补齐 memory-bank / README / 本流进度板。
- 新产物：`apps/frontend/src/components/recommendations/volunteerWorkbenchInsights.ts`、`apps/backend/app/services/_evaluation_batch_stats.py`、`apps/backend/app/services/_workload_calculation.py`。
- 影响说明：三处拆分均保持原页面/接口入口不变，只抽纯 helper / 计算段；评教详情链补回 `mean` 导入后，`test_evaluation_quant_workflow.py` 已恢复通过。
- 当前阻塞：无硬阻塞；若继续扩高考驾驶舱 live 字段，仍需先等 Windows `DB RC1` / contract handoff。
- 下一步：回到业务闭环，优先补更多省份/年份差异解释和其余摘要统一尾项；结构优化继续遵守“小步抽 helper，不做大重构”。

[UTC+08 2026-04-16 17:59]
- 本次完成：继续细化推荐历史对照的年份边界解释；当前已能单独识别“最近样本年份未变、但目标年份切换后变成偏旧样本”的 `stale-only` 场景，并把这类差异同步到页面对比卡、批量对照表、打印/报表摘要和 Excel“风险概览”。
- 新产物：无新增独立文件；增量更新 `recommendationComparison.ts`、`RecommendationSchemeResultsPanel.vue`、`apps/backend/app/exporters/recommendations.py` 及对应前后端测试。
- 影响说明：单方案对比卡与批量对照表现已按“参考口径受影响计数”展示，不再把纯 `stale-only` 差异显示成 `0`；现有跨省 / 同省跨年份导出文案保持不变。
- 当前阻塞：无硬阻塞；若后续要把更多 live 省份规则差异直接落到高考驾驶舱或候选级输出，仍需先等 Windows `DB RC1` / contract handoff。
- 下一步：继续补更多省份/模式组合下的差异解释，优先看哪些边界应直接下沉到候选级、草稿级和报表摘要级，而不是只停留在历史方案对照。

[UTC+08 2026-04-17 21:08]
- 本次完成：排查“服务器打不开”；确认当时前后端都未运行，且单独启动 `npm run frontend:dev` / `npm run backend:dev` 正常；为避免根目录 `npm run dev` 在端口占用或权限异常时进入“半启动后一起退出”，已给 `scripts/dev-local.cjs` 增加 5173/8000 端口预检和 fail-fast 提示。
- 新产物：无新增独立文件；增量更新 `scripts/dev-local.cjs`、`README.md`、memory-bank 和本流同步板。
- 影响说明：以后再遇到端口占用或 `EPERM`，`npm run dev` 会直接报清晰错误，不会先拉起一侧服务再连带把另一侧终止；当前 127.0.0.1:5173 和 127.0.0.1:8000 已恢复可访问。
- 当前阻塞：无硬阻塞；若再出现服务都不在的情况，需要优先判断是未启动还是外部会话把进程带停。
- 下一步：继续回到 Stage B 业务闭环；若用户希望进一步收口启动稳定性，可再补“自动探测并提示现有进程 PID”或更明确的启动器输出。

### `WIN_DB_CORE`
- 待填写

### `WIN_DATA_CURATION_A`
- 待填写

### `WIN_DATA_CURATION_B`
- 待填写
