请在当前仓库中实现《docs/local_edu_tool_dev_spec.md》定义的“本地教务工具”第一阶段到第三阶段（优先完成 P0 范围），并严格遵守仓库根目录的 `AGENTS.md`。

你的执行要求如下：

1. 先阅读 `AGENTS.md` 和 `docs/local_edu_tool_dev_spec.md`。
2. 按里程碑顺序工作，不要一开始实现全部 P1/P2 功能。
3. 先初始化 monorepo：
   - `apps/frontend`：Vue 3 + TypeScript + Vite + Element Plus
   - `apps/backend`：Python 3.11 + FastAPI + SQLite + Alembic
4. 优先完成以下模块：
   - 基础数据中心
   - 学生管理（列表、详情、导入）
   - 教师管理（列表、详情、导入、任教关系）
   - 考试管理与成绩导入
   - 学生/班级/教师成绩分析
   - 课表导入
   - 教师课时与工作量计算
   - Excel 报表导出
   - 本地备份恢复
5. 所有关键计算规则必须配置化，不允许把阈值和系数硬编码到页面中。
6. 附件必须保存在本地目录，不要写入数据库二进制字段。
7. 必须提供：
   - 数据库迁移脚本
   - 示例数据
   - 导入模板生成逻辑
   - Windows 优先的启动脚本
   - 单元测试与基础集成测试
8. 每完成一个里程碑，都要：
   - 更新 README
   - 确保项目可运行
   - 确保核心测试通过
9. 默认只监听 `127.0.0.1`，不要做公网部署。
10. 输出时请明确列出：
   - 已完成内容
   - 尚未完成内容
   - 下一步建议

请直接开始编码，并先完成工程初始化与 P0 的主数据模块。
