# 决策日志

## 2026-04-25：第三轮多路径规划单独建路径与画像表，规则评估采用三态结果

- 决定：新增 `gaokao_pathway`、`gaokao_pathway_rule`、`student_pathway_profile`、`student_pathway_evaluation`，把山东多升学路径、路径规则、学生画像和评估结果从既有普通类冲稳保推荐、`special_type_rule`、`province_volunteer_rule` 中拆出独立底座。
- 原因：第三轮目标是多路径规划，不只是继续加普通类推荐。单招、综评、春考、艺体、体育、提前批等路径很多只能做资格初筛和材料缺口提醒，不能复用普通类位次推荐表述，也不能把特殊类型初筛规则混成录取把握。
- 约束：路径规则引擎必须输出 `passed / failed / unknown`，其中 `unknown` 应被展示为信息不足、材料缺口或人工复核项；D1 的 `bootstrap-shandong` 只提供基础路径和最小边界规则，D2 再补官方规则字典和来源追溯，不要在前端硬编码政策细则。

## 2026-04-25：高考来源追溯单独建 source document / import run，不复用通用 import_job 承载逐行溯源

- 决定：新增 `gaokao_source_document` 与 `gaokao_import_run`，把官方来源页面、本地下载文件、文件 SHA256、解析器名、导入行数、错误报告和 raw 快照路径作为高考数据专用追溯层；通用 `import_job` 仍保留给业务页导入中心聚合。
- 原因：A0 已确认最近三年投档结果已有但来源追溯不足，且 B1 将同时处理投档表、一分一段、省控线等 raw 表和业务表；只靠 `import_job.result_json` 无法稳定给每条高考事实数据保留 `source_document_id`。
- 约束：A1 只登记来源和待解析本地文件，不解析官方 XLS、不写入投档 / 一分一段 / 省控线业务数据；B1 必须复用 `source_document_id / import_run_id` 并补真实 `total_rows / success_rows / failed_rows / skipped_rows / error_report_path`。

## 2026-04-25：质量门禁改为分阶段包装，不降低检查强度

- 决定：把根命令 `npm run check`、`npm run check:e2e`、`npm run check:all` 改为调用 `scripts/quality-gate.cjs`，由脚本按阶段执行原有后端测试、前端 lint、前端单测、前端构建和 Playwright E2E。
- 原因：窗口 8 的目标是让后续多窗口开发能清楚知道“跑到哪一步失败、失败意味着什么、下一步该做什么”。直接用 `&&` 串命令虽然能挡住失败，但对非程序员不够可读，也不利于交接。
- 约束：该包装不能删减测试、不能吞失败、不能降低门禁；后续如果新增质量门禁，优先扩展 `quality-gate.cjs` 的阶段说明，并同步 `docs/codex-task-acceptance-checklist.md`。

## 2026-04-24：导入中心先做只读聚合，不开放无快照的一键回滚

- 决定：新增 `/import-center` 和 `/api/import-center/batches*` 聚合导入模板、批次、错误报告、审计日志和撤销说明；当前不提供自动删除式一键回滚。
- 原因：学生、教师、录取数据、招生计划等导入没有统一保存逐行 before-image、影响主键清单和恢复后重算策略；直接按批次删除可能误删后续手工修正、分析快照或推荐引用。
- 约束：撤销路径先固定为“导入前备份恢复 / 重新导入正确模板覆盖修正 / 回到业务页人工处理”。若未来要做真回滚，必须先补影响行快照、备份强制校验和恢复后重算。

## 2026-04-24：普通用户启动改为后台常驻，`npm run dev` 保留为开发模式

- 决定：新增 `npm run start:local` / `scripts/start-local-services.cjs` 作为普通用户启动入口，后台启动或复用前后端服务并写入 `data/logs/local-services/`；新增 `npm run stop:local` 作为对应停止入口；`start-local-edu.command` 改为调用 `start:local`。
- 原因：用户反复遇到“后端还在、前端打不开”。根因是 `npm run dev` 是前台开发模式，终端关闭后前端 Vite 会停止，而后端可能残留为后台进程，造成状态不一致。普通使用应当允许双击启动后关闭终端窗口，因此需要和开发模式分开。
- 约束：`npm run dev` 仍保留给开发调试使用，因为它能直接显示前后端日志；日常打开系统优先使用 `start-local-edu.command` 或 `npm run start:local`。

## 2026-04-22：应用侧先补齐 Stage B 规则支持表，不越界改 Windows 原始高考库

- 决定：在 `data/app.db` 内新增 `province_score_transform_rule` 与 `subject_requirement_dict` 两张应用侧规则表，并补对应 CRUD / bootstrap / baseline；同时让 `gaokao` 驾驶舱在缺少原始 `gaokao_score_transform_rule` / `gaokao_subject_requirement_dict` 时优先回退到这两类应用侧表。
- 原因：用户明确感知到“数据库还差很多”，而当前应用库只有 `ProvinceVolunteerRule`，Stage B 的赋分规则和选科字典建模仍缺一截；先在应用侧补齐这部分，既能提高当前 SQLite 业务库完整度，也不会越界去改 Windows 数据库权威线的原始 handoff 主库。

## 2026-04-24：特殊类型 fallback 增加初筛优先级，但不改写为录取把握

- 决定：为 `score_line` 与 `plan_only` 两类 fallback 增加 `fallback_priority_score / label / notes`，并让工作台与正式推荐排序优先读取该分数；评分只使用现有可追溯字段，如省控线可用性、批次顺序、计划数、院校层次、职业匹配和章程状态。
- 原因：山东特殊类型当前仍缺完整录取结果，直接平铺计划清单会影响实际使用效率；但“计划存在 / 过省控线”不能被包装成录取概率。因此采用“初筛优先级”作为核看顺序，让系统更可用，同时继续保留强风险提示和边界说明。
- 补充：同日继续把 `fallback_category_label / fallback_review_notes_json` 加入输出，先用专业名、专业类别、来源备注和章程状态识别艺体/春考/综评/单招细分类别；该识别仍是启发式，后续应优先接入可维护字典或更权威 raw 规则来源。

## 2026-04-24：特殊类型类别规则先沉到应用侧规则字典

- 决定：新增 `special_type_rule` 表和 `20260424_0015` 迁移，把山东春考、综评、单招、艺术、体育的细分类别、关键词、核对清单和优先级加成作为应用侧规则字典维护；`fallback_priority` 通过 `SpecialTypeContext` 消费该规则，而不是继续把类别关键词散落在评分函数里。
- 原因：当前 raw 高考计划表能提供 `candidate_type / pathway_code / batch_name`，但没有春考专业类别、综评校测条件、单招职业适应性测试条件等更细字段。先用可重复 bootstrap 的应用侧规则字典补实用性，既比硬编码更可维护，也不会把尚未权威结构化的 raw 信息误写成录取结果。
- 约束：该表仍是“初筛和核对清单”规则，不代表官方完整章程或录取把握；命中类别只用于排序和人工复核提示，不能替代正式填报前逐校核对。

## 2026-04-24：P0 数据健康检查先做成本地命令和可复用后端工具

- 决定：新增 `apps/backend/app/utils/data_health.py` 与 `scripts/check_data_health.py`，通过 `npm run backend:data-health` 统一输出 `data/app.db` 的核心表数量、山东年份覆盖、考生类型覆盖和 P0 缺口摘要，并保留 `--json` 给后续页面复用。
- 原因：交付计划 P0 的首要目标是让数据库缺口可以重复检查、补数据前后可对照，而不是继续靠人工 SQL 或聊天记录判断“当前库是否可用”。先做成本地命令最稳，也能直接服务后续 `/gaokao-data` 覆盖率看板。
- 补充：会改主库的 `merge-handoff`、`materialize-gaokao`、`bootstrap-special-types` 入口都应输出前后健康摘要；其中规则 bootstrap 默认先备份主库，除非显式传 `--no-backup`。

## 2026-04-22：高考 handoff 采用“并入 app.db”而不是继续双库强依赖

- 决定：把 handoff 包里的 `gaokao_*` 原始表、`data_import_error_log`、`score_rank_segment` 同步进 `data/app.db`，并新增 `backend:merge-handoff` 工具；应用启动时若检测到主库已嵌入这些 raw 表，则优先直接走单库。
- 原因：用户明确要求把 Windows 并过来的数据库真正融入项目库中；继续长期维持“应用主库 + 外部高考只读库必选”的双库形态，会让项目数据库一直显得割裂。采用“主库嵌入 raw 高考表，handoff 快照作为同步来源和 fallback”更符合本项目单机 SQLite 主库的定位。

## 2026-04-22：结构化物化分两步走，先普通类，再把特殊类型计划纳入但不误并录取结果

- 决定：把 `gaokao_*` raw 表整理到项目业务表时，先落普通类，再把 `春季高考 / 艺术类 / 单独招生 / 综合评价招生 / 体育类` 扩到 `enrollment_plan`；但 `admission_record` 仍按 raw 结果源的实际覆盖范围保留普通类，不人为编造特殊类型录取结果。
- 原因：当前 raw `gaokao_admission_result` 本身就是普通类口径，而 `gaokao_admission_plan` 已包含多类特殊计划。这样分开处理，既能快速把可用计划数据纳入业务表，又能避免把不存在的特殊类型录取结果硬造进系统。

## 2026-04-22：特殊类型先允许回退参考普通类录取结果，但必须显式打标

- 决定：`spring_exam / independent_recruitment / comprehensive_evaluation` 在缺少专门录取结果时，允许先回退参考普通类录取结果；同时必须补风险标记、工作台提示和报表摘要说明。`art / sports` 不走这条回退，继续保持艺体口径。
- 原因：当前项目需要尽快可用，而这三类特殊计划已进业务表但结果源不足；完全禁用会影响投入使用，但静默混用又会误导判断。因此采用“可回退但显式提示”的折中策略最稳。

## 2026-04-23：山东先按当前招生计划批次补专用规则基线

- 决定：基于当前 `enrollment_plan` 中已出现的山东批次，先给山东 2026 年补普通类、春季高考、艺术类、体育类、单独招生、综合评价招生的专用 `province_volunteer_rule` 基线。
- 原因：山东是当前数据最完整、马上要投入使用的核心省份；先让工作台命中专用规则，比继续停留在全国通用本科批基线更有实用价值。规则仍明确标注为基线，后续需要用官方公告继续精细化。

## 2026-04-23：职业方向和专业映射先做大规模基线初始化，再逐步清洗

- 决定：先批量初始化职业方向和专业到职业路径映射，让 `employment_direction` 和 `major_employment_mapping` 从空表变成可用底座；后续再逐轮清洗过宽、过泛或不够精准的映射。
- 原因：当前职业匹配解释链路已经存在，但底层数据完全为空，直接影响工作台和推荐解释可用性。先把 0 变成 1，比一开始就追求完美映射更符合“尽快投入使用”的目标。

## 2026-04-21：顶层目录先补导航层，不急着大搬家

- 决定：先通过 `docs/README.md`、`docs/dev/README.md`、`scripts/README.md`、`handoffs/README.md` 和根 `README` 的快速导航补清顶层入口，而不是立即重排 `docs/`、`handoffs/`、bundle 目录。
- 原因：当前仓库已存在大量进行中的业务改动，直接搬目录会放大回归面；现阶段更大的问题是“第一次进仓库不知道先看哪”，不是目录名本身不可用。

## 2026-04-21：清理策略只删可再生本地噪音

- 决定：通过 `scripts/clean-local.cjs` 和 `npm run clean:local` 统一清理 `.DS_Store`、`__pycache__`、`.pytest_cache`、`*.egg-info`、`test-results`、`playwright-report`，但不自动清理 `data/`、`handoffs/`、`dist/desktop`、`.venv`、`node_modules`。
- 原因：仓库当前同时承载源码、运行数据、接管包和交付物；“一把梭”式清理风险太高，先把明确可再生的本地噪音稳定收口，才能在不误删有效产物的前提下降低仓库杂乱感。

## 2026-04-05：采用项目 memory-bank

- 决定：在仓库根新增 `memory-bank/` 目录，而不是把项目上下文继续分散在聊天记录里。
- 原因：项目需要跨会话、跨 Codex 交接；Markdown 文件更稳定、可版本化、可审阅。

## 2026-04-05：采用 rulesync 管理项目 AGENTS

- 决定：用 `rulesync` 管理 `AGENTS.md` 的来源文件，而不是直接手改最终生成文件。
- 原因：后续如果要同步更多规则或全局/项目层规则，维护成本更低。

## 2026-04-05：采用 SpecStory 记录 Codex 会话

- 决定：安装 `SpecStory`，并在 zsh 中将裸 `codex` 启动包装到 `specstory run codex`。
- 原因：用户希望跨会话保留历史，但不想手工记忆每次都要走包装命令。
- 约束：带参数启动的 `codex` 仍回退到原始二进制，避免破坏 CLI 参数行为。

## 2026-04-05：暂不引入更重的长期记忆系统

- 决定：当前先不接入 `mem0` 或 `ReMe`。
- 原因：现阶段的核心问题是稳定的全局规则、项目记忆和会话历史，不是复杂的语义记忆服务。

## 2026-04-05：重构策略采用小步拆分

- 决定：后续优先按业务域小步拆分超大页面和超大 service，不进行一次性大重排。
- 原因：当前仓库可运行，直接大改目录会增大回归面和交接成本。

## 2026-04-06：推荐中心先按结果域拆组件

- 决定：`RecommendationsPage.vue` 先抽离“方案结果 / 历史对比 / 批量对照”为独立组件，并把共享类型单独落文件。
- 原因：这一块边界最清晰，能先降低主页面复杂度，同时不改动推荐生成、策略配置和数据维护接口。

## 2026-04-06：前端公共壳层保持后台工具风格

- 决定：前端公共壳层保留清晰分类和页面说明，但不引入过强的流程舞台感、英文品牌或与教务场景不贴合的产品叙事。
- 原因：本项目首先是本地教务后台工具，稳定、直接、易理解比包装感更重要。

## 2026-04-06：优先消除公共层的多重滚动风险

- 决定：公共布局避免使用 `sticky + 固定高度 + 内部 overflow + 内容区视口高度计算` 的组合。
- 原因：这类组合在表格页、标签页和大页面里很容易形成双滚动、错位和滚动体验异常。

## 2026-04-06：评教 service 保持稳定入口、内部拆分

- 决定：保留 `app.services.evaluation` 作为对外 facade，把评教模板、评教批次汇总、班主任量化和共享序列化逻辑拆到私有子模块。
- 原因：`api/routes/evaluation.py`、`reports.py` 和现有测试可继续沿用原导入路径，同时把超大 service 的修改面缩小到具体业务子域。

## 2026-04-06：评教量化页采用组件壳层 + 页面编排

- 决定：`EvaluationQuantPage.vue` 保留数据加载、动作编排和页面级提示，把评教区块、量化区块、弹窗、共享类型与格式化逻辑拆到 `components/evaluation/`。
- 原因：这页同时承载模板管理、批次分析、规则维护、记录编辑和汇总展示；先拆视图与表单层，能在不改接口行为的前提下降低单文件复杂度。

## 2026-04-06：工作量页采用组件壳层 + 页面编排

- 决定：`TimetableWorkloadPage.vue` 保留数据加载、watchers、动作编排和页头提示，把上下文区块、课表区块、规则区块、结果区块和弹窗拆到 `components/workload/`。
- 原因：这页同时承载导入、修正、规则维护、附加项录入、计算和结果明细；先拆视图与表单层，能在不改接口行为的前提下降低单文件复杂度。

## 2026-04-06：前端 UI 先统一壳层与通用节奏

- 决定：先在 `AppLayout.vue` 和 `styles.css` 统一内容宽度、侧栏滚动、tabs、表格壳层、输入控件和通用提示样式，而不是逐页各自修补。
- 原因：当前“散乱”主要来自页面之间的容器密度和控件节奏不一致；先统一壳层和共用样式，后续细调单页成本更低。

## 2026-04-06：推荐页改为页面装配 + 组合逻辑

- 决定：`RecommendationsPage.vue` 保留页面装配和视觉结构，把数据加载、导入导出、推荐生成、历史查看和策略动作下沉到 `useRecommendationsPage.ts`。
- 原因：推荐页的 UI 已基本组件化，继续把请求和动作留在页面主文件只会让维护点分散；先收成组合逻辑，后续再按业务子域细分更稳。

## 2026-04-06：推荐 service 保持稳定入口、内部拆分

- 决定：保留 `app.services.recommendations` 作为对外 facade，把共享/配置、基础库与导入、推荐生成与历史、策略配置拆到私有子模块。
- 原因：`api/routes/recommendations.py`、`reports.py`、`students.py` 和现有测试可继续沿用原导入路径，同时把超大 service 的修改面缩小到具体业务子域。

## 2026-04-06：系统文件下载统一按允许目录校验

- 决定：上传、附件、报表、备份和运行时文件下载统一改为“只允许命中指定目录”，而不是简单依赖 `project_root / file_path`。
- 原因：数据库记录或查询参数一旦出现绝对路径或目录穿越，就可能越出受控目录；按允许目录校验可以同时兼容测试环境的绝对路径和生产环境的相对路径。

## 2026-04-06：系统模板路由只保留一个权威实现

- 决定：删除 `health.py` 中重复定义的 `/system/templates`，保留 `system.py` 中带结构化响应的实现。
- 原因：重复路由会让行为依赖注册顺序，后续维护和调试成本高。

## 2026-04-06：先给高风险前端动作补确认和前置校验

- 决定：优先给系统修复、附件删除、策略模板删除等破坏性动作补二次确认，并给报表导出补前置缺项提示。
- 原因：这类问题直接影响日常使用时的误操作成本，且改动小、回归面可控，比继续做大范围视觉调整更有即时价值。

## 2026-04-06：前端验证体系先走最小闭环

- 决定：先落地“ESLint + Vitest + Playwright 首个跨端冒烟”这一套最小验证闭环，而不是等待完整 E2E 方案一次到位。
- 原因：需求对照文档里最明确的尾项就是前端 `lint`、前端单测和根级 `tests/e2e` 缺失；先把工具链和首个可跑流程建起来，后续扩充成本最低。

## 2026-04-06：工作量页改为页面装配 + 组合逻辑

- 决定：`TimetableWorkloadPage.vue` 只保留页面装配和视觉结构，把数据加载、watchers、请求动作和弹窗状态下沉到 `useTimetableWorkloadPage.ts`。
- 原因：工作量页的模板层已经拆到子组件，继续把编排逻辑留在页面主文件只会让维护点分散；先收成组合逻辑，后续再按导入/规则/结果子域细分更稳。

## 2026-04-06：评教页改为页面装配 + 组合逻辑

- 决定：`EvaluationQuantPage.vue` 只保留页面装配和视觉结构，把模板管理、评教批次分析、量化规则、量化记录和弹窗状态下沉到 `useEvaluationQuantPage.ts`。
- 原因：评教页和工作量页结构相似，当前复杂度的核心已不在模板本身，而在页面级请求动作与状态编排；先收成组合逻辑，后续再按模板/批次/量化子域细分更稳。

## 2026-04-06：E2E 导入链使用仓库内固定 fixture

- 决定：把成绩导入和录取导入的样例文件固定放到 `tests/e2e/fixtures/`，由 Playwright 直接复用，而不是在浏览器测试里临时拼数据文件。
- 原因：导入链路是分析、推荐和报表流程的共同前置；固定 fixture 能显著降低 E2E 的偶发性，也便于后续扩展更多结果输出和异常导入场景。

## 2026-04-06：导入模板错误统一返回业务级 400

- 决定：考试成绩导入遇到模板表头错误时，后端改为返回 `400` 和明确 `detail`，不再让 `ValueError` 直接冒泡成 `500`。
- 原因：这类错误属于用户可修复的输入问题，应该稳定落到前端提示链路，方便 E2E 覆盖，也降低真实使用时的排查成本。

## 2026-04-06：推荐页改为 facade + 子组合逻辑

- 决定：`useRecommendationsPage.ts` 不再继续承载全部推荐逻辑，改为 facade，把录取库/目录维护下沉到 `useRecommendationCatalogManager.ts`，把推荐生成、历史、策略和导出下沉到 `useRecommendationWorkflow.ts`。
- 原因：推荐页已经是当前前端剩余复杂度最高的组合逻辑之一；先把子域边界切出来，比继续在单个 900 行文件里迭代更稳，也更便于后续继续按历史对比或导出细分。

## 2026-04-06：推荐工作流再拆为生成与历史两个子域

- 决定：`useRecommendationWorkflow.ts` 不再继续承载生成、策略、历史对比和导出全量逻辑，改为 facade，把生成/策略下沉到 `useRecommendationGenerationManager.ts`，把历史/对比/导出下沉到 `useRecommendationHistoryManager.ts`。
- 原因：推荐工作流虽然已经从页面主文件中抽离，但单个组合文件仍有 600 行量级；继续把生成链和历史链分开，后续维护和继续细分的成本更低。

## 2026-04-06：推荐历史链再拆为列表与对比两个子域

- 决定：`useRecommendationHistoryManager.ts` 不再继续承载历史查询、当前方案查看、历史对比和导出全量逻辑，改为 facade，把列表/查看下沉到 `useRecommendationHistoryCollection.ts`，把对比/导出下沉到 `useRecommendationSchemeComparison.ts`。
- 原因：当前推荐历史链已经和结果展示、导出、E2E 回归强耦合；把“拿历史数据”和“做方案对比/导出”拆开，后续补更复杂对比场景时修改面更小。

## 2026-04-06：推荐方案对比链再拆为单对比、多对比和导出三个子域

- 决定：`useRecommendationSchemeComparison.ts` 不再继续承载单方案对比、多方案对比和导出全量逻辑，改为 facade，把单方案对比下沉到 `useRecommendationSingleComparison.ts`，把多方案对比下沉到 `useRecommendationMultiComparison.ts`，把导出下沉到 `useRecommendationSchemeExport.ts`。
- 原因：推荐对比链直接连接结果展示、导出和 E2E 回归，后续如果要补更复杂差异视图或导出策略，拆成最小子域能显著降低回归面。

## 2026-04-06：推荐生成链再拆为提交与策略两个子域

- 决定：`useRecommendationGenerationManager.ts` 不再继续承载学生/考试选项加载、推荐提交、策略设置和策略模板全量逻辑，改为 facade，把提交链下沉到 `useRecommendationSubmissionManager.ts`，把策略链下沉到 `useRecommendationStrategyManager.ts`。
- 原因：当前推荐页剩余复杂度已经主要集中在生成链；把“提交生成”和“策略维护”拆开，可以降低后续改策略模板或生成参数时的交叉回归面。

## 2026-04-06：推荐策略链再拆为设置与模板两个子域

- 决定：`useRecommendationStrategyManager.ts` 不再继续承载阈值/黑白名单设置和策略模板维护全量逻辑，改为 facade，把设置链下沉到 `useRecommendationStrategySettings.ts`，把模板链下沉到 `useRecommendationStrategyPresets.ts`。
- 原因：策略链既涉及参数校验，也涉及模板复用与删除确认，后续变更频率高；继续拆成更小的子域可以减少改动时的回归面。

## 2026-04-07：打印优化先从推荐报告和成长档案摘要切入

- 决定：先给推荐报告和成长档案摘要补独立打印预览页，并在推荐结果页、成长档案页、报表中心增加“打印预览”入口，而不是一次性覆盖所有报表类型。
- 原因：开发文档里的 `PDF/打印排版优化` 目前完全空白；推荐报告和成长档案摘要最接近“单页材料”，数据面也最稳定，适合先做可交付闭环。

## 2026-04-07：学生成绩分析单也接入打印预览

- 决定：继续把 `student_analysis` 接入报表中心打印预览，并新增独立打印页，而不是只停留在推荐/成长档案两个场景。
- 原因：学生成绩分析单是开发文档里报表中心最基础的高频输出之一；把它纳入打印优化后，打印能力就不再只是边缘功能，而是开始覆盖核心教学分析材料。

## 2026-04-07：班级/年级/教师分析打印预览一批次补齐

- 决定：沿同一套打印框架，在同一批次里补齐 `class_analysis`、`grade_summary`、`teacher_analysis` 三类打印预览，而不是逐个零散推进。
- 原因：这三类都复用现有分析接口和相近的表格结构；同批落地能更快形成一致的打印体验，也能一次性降低开发文档中打印优化的剩余差距。

## 2026-04-07：工作量 / 评教 / 量化打印预览也纳入同一框架

- 决定：继续沿同一套打印框架，在同一批次里补齐 `teacher_workload`、`evaluation_summary`、`adviser_quant_summary` 三类打印预览，并同步增强 E2E 测试后端演示数据。
- 原因：这样可以直接把开发文档里报表中心 9 类核心输出全部纳入打印能力，避免打印优化长期停在“部分完成”。

## 2026-04-07：多学年全景对比先从年级视角落第一版

- 决定：先做年级视角的多学年全景对比接口和分析中心标签页，展示学年摘要、考试时间轴和学科趋势，而不是一开始就覆盖学生/班级/教师全域全景。
- 原因：开发文档里的“多学年全景对比”目前完全空白；年级视角最接近现有快照结构，也最能复用现有分析模型，适合先形成真实可用闭环。

## 2026-04-07：复杂分析看板先复用现有 panorama 数据继续增强

- 决定：复杂分析看板的下一步先在现有 `GradePanoramaPanel.vue` 上增加学科攻坚优先级和风险预警，而不是马上扩新后端接口或新增独立分析页。
- 原因：现有年级全景对比数据已经足够派生出行动导向的排行与预警；先在前端完成第二层看板，可以用更小改动把 P2 的“更复杂分析看板”从空白推进到部分完成，同时不给后端契约增加新负担。

## 2026-04-07：全景对比第二个业务域先落班级视角

- 决定：在年级 panorama 基础上，下一步先补班级视角全景对比接口和分析中心“班级全景”标签页，而不是直接跳到教师视角或新建独立分析模块。
- 原因：班级视角与现有成绩快照和全景看板结构最接近，复用成本最低；先补它可以更快把“多业务域全景对比”从单点能力推进到可感知的多视角能力。

## 2026-04-07：教师视角沿同一套 panorama 框架继续补齐

- 决定：在年级、班级之后继续沿同一套 panorama 契约补 `teachers/{teacher_id}/panorama` 和“教师全景”标签页，而不是新开独立教师长期分析模块。
- 原因：教师视角仍能复用现有学年摘要、时间轴和攻防/预警看板逻辑；保持同一框架可以更快把分析中心从单点增强推进成真正的多业务域全景能力。

## 2026-04-07：桌面打包第一版采用 Electron 壳 + Python 后端独立二进制

- 决定：桌面打包先采用 `Electron` 作为本地壳层，复用现有 Vue 前端和 FastAPI 后端；后端通过 `PyInstaller` 打成独立二进制，再由 Electron 启动本地 API 和静态前端资源。
- 原因：当前项目本质上已经是本地 Web 应用，最小成本的桌面化路径就是保留前后端架构不动，只新增桌面启动和打包层；这样可以最快把开发文档中的“桌面打包”从空白推进到真实产物。

## 2026-04-07：桌面打包第二步先补 Windows 目标配置和桌面菜单

- 决定：在已有 macOS `dir` 产物基础上，先补 Windows `dir` / `nsis` 目标配置，以及 Electron 桌面菜单中的数据目录/导出目录入口，而不是马上投入签名或安装器美化。
- 原因：当前最缺的是“可打包”和“可用”的基础能力；先把跨平台目标和本地数据入口补齐，能显著降低后续实机验证和日常使用成本。

## 2026-04-21：高考主线库采用独立双库接入，不替换应用库

- 决定：保留 `data/app.db` 作为应用主库；高考只读驾驶舱与 handoff 相关能力单独读取 `data/local_edu_tool/local_edu.sqlite3`，通过 `LOCAL_EDU_GAOKAO_DB_PATH` 可覆盖路径。
- 原因：接管包内 `local_edu.sqlite3` 是独立高考主线库，表结构与当前应用库不同，直接替换会破坏学生、考试、推荐历史等现有业务数据。

## 2026-04-21：外部数据库接管包统一收口到 `handoffs/`

- 决定：把根目录的数据库接管包整理到 `handoffs/<bundle-name>/`，运行入口只保留 `data/local_edu_tool/local_edu.sqlite3`。
- 原因：这样既能保留交接文档、指标和工具链的原始包结构，又能避免仓库根目录继续堆积大体积数据库资产。

## 2026-04-09：推荐生成链继续拆成提交 helper、候选筛选、结果构建和历史子域

- 决定：前端把推荐提交前置校验与 payload 规范化下沉到 `recommendationSubmission.ts`；后端把 `_recommendations_generation.py` 继续拆为 `_recommendations_candidates.py`、`_recommendations_result_builder.py` 与 `_recommendations_history.py`，保留 `recommendations.py` facade 和现有 API 契约不变。
- 原因：v2 文档要求先完成推荐域收尾，再进入“高考志愿报名辅助”主线；如果继续把提交校验、候选筛选、解释拼在同一层，后面接招生计划、志愿模式和就业方向排序时会再次快速膨胀。

## 2026-04-09：高考志愿 S1 先接在现有 recommendations 业务域内

- 决定：不立即拆新的一级路由或整套志愿子路由，先在现有 `/recommendations` 页内增加“招生计划库”和“省份规则”两块，并在后端沿用 `app.services.recommendations` facade 扩出 `EnrollmentPlan` 与 `ProvinceVolunteerRule`。
- 原因：当前推荐域刚完成一轮收尾，继续沿同一业务域做 S1 数据底座能最大化复用院校库、专业库、录取库和推荐策略，不会把导航、接口测试和用户心智一次性拉大。

## 2026-04-09：高考志愿 S3 先做 preview 接口 + 前端本地草稿

- 决定：学生志愿工作台第一段先不急着落数据库表或新接口集合，优先补 `/api/recommendations/volunteer-workbench/preview` 和页内本地草稿编辑；后端负责把招生计划、录取基线和省份规则聚成候选池，前端先负责排序与暂存草稿。
- 原因：当前最缺的是“学生 -> 候选池 -> 志愿表”这条最小闭环，而不是最终持久化模型；先把预览和编辑动作跑通，能最快验证产品方向，也能避免在规则还未稳定时过早固化数据结构。

## 2026-04-09：志愿草稿持久化采用独立 draft 表，而不是继续塞进 ConfigItem

- 决定：学生志愿工作台第二段不复用 `ConfigItem` 或浏览器本地存储做长期保存，而是新增 `VolunteerDraft` / `VolunteerDraftItem` 表和 `/api/recommendations/volunteer-drafts*` 接口，草稿项按 `candidate_snapshot_json` 保存快照。
- 原因：草稿已经从页面临时状态升级为需要跨刷新、跨会话恢复的业务对象；独立 draft 表更利于后续做导出、打印、历史版本和筛选参数追溯，也避免把结构化数据继续挤进通用配置表。

## 2026-04-09：志愿草稿打印 / 导出继续复用现有 reports + print 框架

- 决定：学生志愿工作台第三段不再单独新开一套导出接口或下载页，而是沿用既有 `/api/reports/export`、`ReportExportRecord` 和 `/print/*` 体系，新增 `volunteer_draft_summary` 报表类型与 `VolunteerDraftPrintPage.vue`。
- 原因：推荐报告、成长档案和报表中心已经在同一套导出 / 打印框架内运行稳定；志愿草稿继续复用这套模式，能减少前后端分叉，也便于后续统一做导出记录回看和 E2E 覆盖。

## 2026-04-16：高考数据驾驶舱先走只读接口 + fallback

- 决定：高考数据驾驶舱第一版不等待 Mac 侧冻结 schema，也不改数据库结构；后端优先读取已有 `gaokao_*` 原始表，若当前环境无对应表或字段，则回退到 `docs/dev/02_SYNC_BOARD_2026-04-16.md` 的基线数据和现有应用模型。
- 原因：当前 Windows 数据库线仍是权威，Mac 侧任务优先是“可读可审阅”的驾驶舱，而不是本地擅自扩 schema；只读 + fallback 能在不越界的前提下先把页面、接口和空态跑通。

## 2026-04-16：分析类导出优先复用现有 analytics 口径

- 决定：`grade_summary` 导出不再在 `reports.py` 里单独重算一套年级统计，而是直接复用 `analytics_service.get_grade_analytics()`；`teacher_analysis` 导出摘要只补与打印页对齐的最小字段，不另起新统计链。
- 原因：用户要求“打印和导出尽量共用公共结构，不重新发明新口径”；直接复用现有 analytics 返回能减少口径分叉，也让打印页、报表中心与 Excel 导出更容易保持一致。

## 2026-04-09：志愿表编辑器先补前端“冲稳保视图”，不扩新接口

- 决定：先在现有学生志愿工作台草稿区新增“批次顺序 / 冲稳保视图”切换，用前端 helper 按 `result_type` 分组展示草稿，不新增后端接口或新的持久化字段。
- 原因：开发文档 `6.6 模块 F：志愿表编辑器` 明确要求支持“按批次视图 / 冲稳保视图查看”；当前候选与草稿数据已经携带 `result_type`，直接在前端补视图切换可以最快闭合文档要求，同时避免过早扩大接口面。

## 2026-04-09：代码字段显示沿现有 preview / draft snapshot 扩展，不新开专用查询

- 决定：继续沿现有工作台 preview、草稿快照、打印页和 `volunteer_draft_summary` 导出链路，把 `college_code_snapshot`、`major_code_snapshot` 和 `major_group_code` 直接透传并展示，不新增单独的代码查询接口。
- 原因：开发文档 `6.6 模块 F：志愿表编辑器` 已明确要求显示院校代码 / 专业代码 / 专业组代码，而招生计划快照本身已经携带这些字段；直接沿现有 snapshot 数据面扩展，改动最小，也能保证页面、打印和导出三处信息一致。

## 2026-04-23：山东特殊类型先用 raw `gaokao_score_line` 做资格初筛，不把省控线伪装成录取线

- 决定：当 `art / sports` 缺少专门录取结果时，工作台预览与正式推荐生成不再直接返回空，而是读取主库 raw `gaokao_score_line`，按山东省控线做“资格初筛” fallback；结果统一打 `score_line_reference_only`，跨目标年份回退时再打 `cross_year_score_line_reference`。
- 原因：当前主库已经有山东艺术类 / 体育类控制线，但 raw `gaokao_admission_result` 仍基本只有普通类口径；如果继续等待特殊类型录取结果补齐，艺体链路会长期停在“有计划、无结果”。用省控线先做初筛，能把系统推进到“可筛、可解释、可提示风险”的状态，同时通过强提示避免把省控线误读成院校专业录取把握。

## 2026-04-09：拖拽排序先用批次视图下的原生 HTML5 拖放，不引入新依赖

- 决定：学生志愿工作台的拖拽排序先只在“批次顺序”视图下接入原生 HTML5 拖放，沿现有 `VolunteerDraftItem[]` 本地顺序直接重排，同时保留“上移 / 下移”作为精调和兜底。
- 原因：仓库当前没有可复用的拖拽库，而开发文档 `6.6 模块 F：志愿表编辑器` 已明确要求支持拖拽排序；直接用原生拖放可以最快补齐能力，又不需要新增依赖或改后端接口，风险最小。

## 2026-04-09：另存为 / 草稿对比先复用现有草稿详情接口，不扩新后端 API

- 决定：学生志愿工作台的“另存为新草稿”继续复用现有 `/api/recommendations/volunteer-drafts` 创建接口，草稿对比则直接在前端加载另一份 `VolunteerDraftDetail` 后计算新增 / 移除 / 顺序变化 / 分层变化，不新增专用 compare API。
- 原因：开发文档 `6.6 模块 F：志愿表编辑器` 要求的是保存方案 / 另存为 / 方案对比，但当前草稿详情接口已经包含筛选参数、规则快照和完整条目顺序；前端基于两份草稿快照直接比对，能用最小改动补齐能力，也避免后端过早固化新的比较契约。

## 2026-04-09：就业增强列先复用 Major 画像字段做推断，不提前引入 S4 新模型

## 2026-04-10：批量推荐默认沿学生档案生源地分别生成，前端只把一致省份作为自动建议

- 决定：`/api/recommendations/batch-generate` 在未手工指定统一 `province` 时，继续按每名学生档案中的 `origin_province` 分别生成方案；前端批量模式只在“所选学生生源地完全一致”时自动带入省份，且该自动值在从单学生切到批量时不再保留为强制统一省份。
- 原因：`gaokao_dev_bundle_v3/gaokao_dev_doc_v3.md` 明确要求批量推荐支持不同生源地学生混合生成，并要求能体现同校同专业在不同省份的结果差异；把自动带入和手工统一分开，才能避免 UI 状态误把批量场景收窄成单省查询。

- 决定：志愿表编辑器中的“就业方向相关增强”先复用 `Major.direction`、`Major.career_path` 和 `Major.note`，在工作台 candidate snapshot 中透传原始画像，再由前端 `buildVolunteerEmploymentProfile` 推断“对应就业方向 / 匹配强度 / 需读研 / 需资格证 / 说明摘要”；不在这一步提前引入就业方向库或专业-就业方向映射表。
- 原因：开发文档 `6.6 模块 F` 当前要求的是先把就业相关列带进志愿表，而真正的“目标就业方向输入 / 映射 / 排序增强”属于后续 S4 能力；先复用现有专业画像字段，能以最小改动把页面、草稿快照和解释层打通，同时避免过早固化尚未设计完的数据模型。

## 2026-04-09：S4 第一段先在推荐主页面内补就业方向库和专业就业映射

- 决定：S4 第一段先新增 `EmploymentDirection` / `MajorEmploymentMapping`、`20260409_0010_employment_direction_schema.py` 和 `/api/employment-directions`、`/api/major-employment-maps*`，前端继续留在 `RecommendationsPage.vue` 内，用“就业方向库 / 专业就业映射”两个标签页承载，不提前拆独立页面或接学生职业意向模型。
- 原因：开发文档 `12.3` 的前两项验收是“能维护就业方向库”和“能维护专业-就业方向映射”；先把标准化方向对象和映射关系做成稳定数据底座，能最快闭合文档要求，也能为后续学生职业意向输入、排序增强和结果解释提供可复用基座。

## 2026-04-10：学生职业意向先做“学生级偏好 + 工作台/草稿同步”，不额外拆新页面

- 决定：S4 第二段先新增 `StudentCareerPreference` 和 `/api/students/{id}/career-preference`，前端仍留在学生志愿工作台内录入职业意向；工作台表单与 `VolunteerDraft` 同步复用同一组字段，不提前拆独立 `CareerPreferencePage`。
- 原因：当前最紧迫的后续能力是“排序增强”和“结果解释”，它们都需要稳定、可持久化且可被草稿追溯的职业意向输入；先把职业意向沉到学生级对象，并让工作台和草稿共用字段，能以最小改动为后续排序与解释提供一致输入面，同时避免再扩一个独立页面增加导航和维护负担。

## 2026-04-10：职业匹配评分统一沉到 result builder，并让正式推荐默认复用学生偏好

- 决定：S4 第三段不再分别在工作台 preview、推荐生成和结果页各写一套“职业匹配”逻辑，而是把职业匹配评分、排序 key、解释摘要和路径风险统一下沉到 `_recommendations_result_builder.py`；工作台 preview 使用当前表单的职业意向，正式推荐生成默认复用 `StudentCareerPreference` 做二次排序增强。
- 原因：开发文档 `6.5 模块 E` 明确要求“录取可行性优先、职业意向增强、不反客为主”；统一沉到结果构建层可以保证工作台和正式推荐遵守同一排序标准，同时避免一处改解释、一处改排序导致链路分叉，也不需要为正式推荐额外扩新的输入表单。

## 2026-04-10：Stage B 的模拟分展示统一复用结果快照元信息

- 决定：推荐历史、打印页和导出概况不再各自推断“模拟模式”文案，而是统一复用 `RecommendationResult.snapshot_json` 中的 `target_year / score_input_label / score_confidence / reference_exam_name / use_historical_mapping`。
- 原因：Stage B 现在同时涉及推荐历史、打印预览、推荐报告和志愿草稿导出；把模拟分元信息稳定沉到结果快照，可以避免前后端各处再从 `rule_json` 或 `reason_text` 反推，链路更一致，也更利于 E2E 断言。

## 2026-04-10：采用 gaokao_dev_bundle_v3 的阶段顺序，先收尾旧推荐中心再扩新建模

- 决定：在 `gaokao_dev_bundle_v3/gaokao_dev_doc_v3.md` 引入后，后续开发优先级改为先完成 Stage A 的旧推荐中心尾项，再进入省份规则中心、生源地、预估分数等正式建模阶段；不再默认继续把更多展示层能力直接堆到当前 S4 链路上。
- 原因：v3 文档把“先收尾旧推荐中心、再升级为高考志愿报名辅助台”的阶段顺序写成强约束；如果继续只沿 S4 展示面补功能，会让 README、Windows 验证、旧推荐中心 E2E 和规则中心正式建模继续滞后。

## 2026-04-10：Stage B 省份规则先做“基线装载 + 一键补齐”，不直接写死完整官方细则

- 决定：全国省份规则第一版先以 `ProvinceVolunteerRule` 的“本科批起始规则”落库，后端提供 `POST /api/province-volunteer-rules/bootstrap` 和 `seed_reference_data()` 自动补齐，前端在“省份规则”页补一键装载入口；已存在的同键规则默认跳过，不做覆盖刷新。
- 原因：当前最需要的是把 Stage B 从“手工建空表”推进到“新库可直接起跑”，而不是一次性把每省每年的完整官方细则写死；采用“起始基线 + 后续人工细化”的策略，既能降低初始化成本，也能避免用不够稳的默认值覆盖已有人工配置。

## 2026-04-10：学生生源地先沉到 Student 主档，推荐生成与工作台共用一套分数输入语义

- 决定：Stage B 第二段先在 `Student` 上新增 `origin_province`，并让推荐中心单个生成在未手填省份时回退到学生档案；同时把 `score_input_mode / reference_exam_name / use_historical_mapping / risk_preference` 的解释与校验统一抽到 `_recommendations_score_input.py`，由正式推荐和工作台共用，而不是各写一套。
- 原因：v3 文档已把“生源地、省份规则、预估分数”列为必须一起建模的输入层；先把生源地沉到学生主档，才能让推荐和工作台共享稳定默认值；再把分数输入语义统一到一个 helper，能避免推荐中心和工作台对同一模式给出不同风险标记或解释文案。

## 2026-04-10：根目录后端操作统一走 Node 包装器，而不是要求手工激活虚拟环境

- 决定：在仓库根新增 `scripts/backend-cli.cjs`，统一提供 `npm run backend:migrate`、`backend:init-demo`、`backend:test` 与 `backend:dev`，由脚本自动解析 `.venv/bin` 或 `.venv/Scripts` 下的可执行文件。
- 原因：当前根目录已经有前端、桌面和 E2E 统一入口，但后端常用操作仍依赖手工激活虚拟环境和记忆子目录命令；用一个轻量 Node 包装器统一入口，能降低日常维护和跨会话交接成本，同时避免为了这类命令引入更重的任务编排系统。

## 2026-04-10：规则命中与招生计划过滤共用同一套模式兼容定义

- 决定：把 `3+1+2` 与 `物理类 / 历史类` 的兼容集合抽到 `app.repositories.recommendations.build_compatible_exam_modes()`，由工作台规则匹配和招生计划候选查询共同复用，而不是各自维护一套兼容逻辑。
- 原因：Stage B 年份边界解释已经允许“兼容模式预览”，如果招生计划过滤仍按精确 `exam_mode` 查询，前端会出现“提示可兼容，但候选池为空”的假阴性；统一 helper 可以把规则说明和真实候选结果收成同一行为。

## 2026-04-12：根目录统一开发启动入口优先走轻量 Node 包装器

- 决定：在保留 `scripts/dev.sh` / `scripts/dev.ps1` 的同时，新增根目录 `npm run dev` 和 `scripts/dev-local.cjs`，用同一条命令并行启动 `backend:dev` 与 `frontend:dev`。
- 原因：现有仓库已经把后端迁移、初始化和测试收口到根目录 npm scripts，但开发启动仍要记两条命令或平台脚本；补一条轻量统一入口后，跨会话启动和交接成本更低，也能减少根目录 `npm run` 参数透传带来的混淆。

## 2026-04-16：高复杂模块继续按“抽纯 helper + 保持入口不动”拆分

- 决定：Stage B 工作台边界/规则摘要、评教批次统计、工作量逐教师计算继续按“小步抽纯 helper”的方式分别下沉到 `volunteerWorkbenchInsights.ts`、`_evaluation_batch_stats.py`、`_workload_calculation.py`；原有页面、service facade、路由和 schema 入口保持不变。
- 原因：当前仓库工作区较脏，且 P0/P1/P2/P4 刚完成一轮业务收口；相比大范围重构，优先抽离纯计算/聚合段更可逆，也更容易用现有定向工作流测试锁住行为不变。

## 2026-04-17：统一开发入口改为先做端口预检再启动

- 决定：`scripts/dev-local.cjs` 在拉起 `backend:dev` / `frontend:dev` 前，先预检 `127.0.0.1:8000` 和 `127.0.0.1:5173`；遇到端口占用或 `EPERM` 时，直接输出清晰错误并退出。
- 原因：此前根目录 `npm run dev` 在端口占用或受限上下文下会进入“前端换端口 / 后端失败 / 双方一起退出”的状态，表面上像“服务器打不开”，但信息不够直接；改成 fail-fast 后，排障成本更低，也避免误杀已正常运行的服务。
- 补充：2026-04-24 已把该策略继续改为“先健康检查再端口预检”；如果 `5173/8000` 已经是本项目服务，则直接复用并正常退出，避免用户重复启动时被端口占用提示误导。

## 2026-04-24：阶段二规则字典页签采用懒加载

- 决定：`special_type_rule`、`province_score_transform_rule`、`subject_requirement_dict` 三类规则字典在 `/recommendations` 中按页签激活后再加载，不放进推荐页初始 `Promise.all`。
- 原因：这三类表主要用于规则核对和维护，不是普通推荐主流程的首屏前置条件；懒加载可以减少推荐页首次进入时的数据库和前端渲染压力，同时仍保留页面内 bootstrap 与筛选核对入口。

## 2026-04-24：推荐页学生选项临时提高到本地单校 1000 条上限

- 决定：推荐页的学生选项从 `/api/students?page_size=200` 提高到 `page_size=1000`，后端学生列表接口同步允许 `page_size<=1000`。
- 原因：真实本地库可能已有数百名学生，200 条首屏选项会导致推荐/志愿工作台选不到后排学生；当前项目定位是本地单用户、单校工具，1000 条上限比立即引入远程搜索改动更小。后续如果学生规模继续变大，再把推荐页学生选择器改成远程搜索。

## 2026-04-24：阶段一数据补齐先以真实覆盖矩阵和审计摘要暴露缺口

- 决定：阶段一不伪造缺失年份或特殊类型录取结果，而是在 `data_health.py` 和 `/api/gaokao/data-health` 中输出按年份 / 类别 / 批次的覆盖矩阵、缺少年份和 `audit_summary`，再由 `/gaokao-data` 的“山东覆盖”页签展示。
- 原因：当前真实主库已经有普通类录取 2020-2025、招生计划 2020/2024/2025、特殊类型计划、2024/2025 一分一段和省控线，但仍缺部分年份与特殊类型专门录取结果；把缺口作为一等输出，比用占位数据填平更符合“不造数据、不静默 fallback”的交付红线，也能支持后续真实导入前后对照。
